# Core dependencies for Dispatch Center Service
# 使用 kafka-python 替代 confluent-kafka（不需要系统依赖，适合 Render 部署）
kafka-python==2.0.2
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
websockets==12.0
# 使用 pydantic 1.10.x（纯 Python，不需要 Rust 编译，兼容 FastAPI 0.104.1）
pydantic==1.10.13

