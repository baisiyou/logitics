# 简化版依赖（不需要系统库，但功能可能受限）
# 如果 confluent-kafka 无法安装，可以使用这个版本
# 注意：kafka-python 的性能不如 confluent-kafka

# 使用 kafka-python-ng 替代 kafka-python（支持 Python 3.12+，修复了兼容性问题）
kafka-python-ng==2.2.3

# Web Framework
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
websockets==12.0
# 使用 pydantic 1.10.x（纯 Python，不需要 Rust 编译）
pydantic==1.10.13

